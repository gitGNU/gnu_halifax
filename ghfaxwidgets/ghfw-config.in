#! /bin/sh
# Originally from libxml, Copyright (C) Daniel Veillard
# Modifications for neon Copyright (C) 2000 Joe Orton.
# Modifications for libghfaxwidgets Copyright (C) 2001 Wolfgang Sourdeau.

prefix=@prefix@
exec_prefix=@exec_prefix@
includedir=@includedir@

usage()
{
    cat <<EOF
Usage: ghfw-config [OPTION]

Known values for OPTION are:

  --prefix=DIR		change ghfw prefix [default $prefix]
  --libs		print library linking information
  --cflags		print pre-processor and compiler flags
  --help		display this help and exit
  --version		output version information
EOF

    exit $1
}

if test $# -eq 0; then
    usage 1
fi

cflags=false
libs=false

while test $# -gt 0; do
    case "$1" in
    -*=*) optarg=`echo "$1" | sed 's/[-_a-zA-Z0-9]*=//'` ;;
    *) optarg= ;;
    esac

    case "$1" in
    --prefix=*)
	prefix=$optarg
	;;

    --prefix)
	echo $prefix
	;;

    --version)
	echo ghfaxwidgets @GHFW_VERSION@
	exit 0
	;;

    --help)
	usage 0
	;;

    --cflags)
       	echo @GHFW_INCLUDEDIR@ @GHFW_CFLAGS@
       	;;

    --libs)
       	echo @GHFW_LIBDIR@ @GHFW_LIBS@
       	;;

    *)
	usage
	exit 1
	;;
    esac
    shift
done

exit 0
