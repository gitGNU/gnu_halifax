# MANPAGE should be defined in configure.in and include ghfaxviewer.1 gfax.1
# as needed. It is put as a macro so that each package can define its own
# set of manpages while sharing Makefile.am across the whole project.

man_MANS = halifax.1 @MANPAGES@

EXTRA_DIST = README README.cvs $(man_MANS)
